version: 2

sources:
  - name: test_macro
    database: "{{ target.database }}"
    schema: "{{- target.schema -}}_{{ target.user }}{{ pipeline_string() }}"
    tables:
      - name: source
        identifier: raw_source_table`