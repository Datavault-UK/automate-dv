version: 2

models:
  - name: MULTI_ACTIVE_SATELLITE
    tests:
      - expect_tables_to_match:
          expected_seed: multi_active_satellite_expected_seed
          unique_id: CUSTOMER_PK
          compare_columns:
            - CUSTOMER_PK
            - HASHDIFF
            - CUSTOMER_NAME
            - CUSTOMER_PHONE
            - EFFECTIVE_FROM
            - LOAD_DATE
            - SOURCE
