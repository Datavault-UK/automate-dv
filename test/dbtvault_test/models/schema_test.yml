version: 2

models:
  - name: MULTI_ACTIVE_SATELLITE_NO_PK_CDK_HASHDIFF
    tests:
      - assert_data_equal_to_expected:
          expected_seed: multi_active_satellite_no_pk_cdk_hashdiff_expected_seed
          unique_id: CUSTOMER_PK
          compare_columns:
            - CUSTOMER_PK
            - HASHDIFF
            - CUSTOMER_NAME
            - CUSTOMER_PHONE
            - EFFECTIVE_FROM
            - LOAD_DATE
            - SOURCE
