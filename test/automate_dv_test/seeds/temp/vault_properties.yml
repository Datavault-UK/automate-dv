version: 2
seeds:
  - name: multi_active_satellite_expected_seed
    config:
      column_types:
        CUSTOMER_PK: BYTEA
        CUSTOMER_NAME: VARCHAR
        CUSTOMER_PHONE: VARCHAR
        HASHDIFF: BYTEA
        EFFECTIVE_FROM: DATE
        LOAD_DATE: DATE
        SOURCE: VARCHAR
      quote_columns: false
