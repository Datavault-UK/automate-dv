version: 2
seeds:
  - name: raw_source
    config:
      CUSTOMER_PK: BINARY(16)
      BOOKING_FK: BINARY(16)
      ORDER_FK: BINARY(16)
      LOAD_DATE: DATE
  - name: raw_source_2
    config:
      CUSTOMER_PK: BINARY(16)
      BOOKING_FK: BINARY(16)
      ORDER_FK: BINARY(16)
      LOAD_DATE: DATE