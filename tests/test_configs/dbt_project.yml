analysis-paths:
- analysis
clean-targets:
- target
- dbt_modules
data-paths:
- data
macro-paths:
- macros
models:
  snowflake_demo:
    feature_sql_files:
      enabled: false
      materialized: incremental
    hub_sat_link_load:
      enabled: true
      materialized: incremental
    source_creation:
      enabled: true
      materialized: incremental
    star_schema:
      enabled: true
      materialized: view
    stg:
      enabled: true
      materialized: view
  vars:
    date: 1993-01-02
    history_date: 1993-01-01
name: snowflakeDemo
profile: my-snowflake-db
source-paths:
- models
target-path: target
test-paths:
- tests
version: '1.0'
